<table class="table table-hover">
     <thead >
       <tr>
         <th>Nome</th>
         <th>Preço</th>
         <th></th>
       </tr>
     </thead>
     <tbody>
       <% @products.each do |product| %>
         <tr>
           <td><%= product.name %></td>
           <td>R$ <%= product.price %></td>
           <td><%=link_to 'Adicionar ao Carrinhos',{:controller => "carts", :action => "addtocart",:product => product.id},class: "btn btn-success" %></td>
           
         </tr>
      <%end%>
    </tbody>
 </table>