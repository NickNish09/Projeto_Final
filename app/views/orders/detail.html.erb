<h1>Detalhes do pedido número <%= @order.id %></h1>
<table>
	
	

</table>	
<table class="table table-hover">
     <thead >
       <tr>
         <th>Nome</th>
         <th>Total</th>
         <th>Quantidade</th>
       </tr>
     </thead>
     <tbody>
       <% @order.items.each_with_index do |h,idx| %>
		<tr>
			<td><%= @products[idx] %></td>
			<td>R$ <%= '%.2f'%((h["quantity"].to_f)*(h["price"].to_f)) %></td>
			<td><%= h["quantity"] %></td>

		</tr>

		<% end %>
    </tbody>
</table>

<p>Total: R$ <%= '%.2f'%(@order.total) %></p>
<% if current_user.admin? %>
<%= link_to 'Voltar',orders_path,class: "btn btn-primary" %>
<% else %>
<%= link_to 'Voltar',order_path(current_user) ,class: "btn btn-primary" %>
<% end %>